apiVersion: v1
kind: Service
metadata:
  name: {{ include "anime-reminder.fullname" . }}
  labels:
    {{- include "anime-reminder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anime-reminder.selectorLabels" . | nindent 4 }}
